import React from "react";
import { gql, useQuery } from "@apollo/client";

const Home = () => {
  // const {loading, error, data} = useQuery()

  //     if(loading){
  // //
  //     }
  //     if(error){
  //         //
  //     }

  return <div>Home</div>;
};

export default Home;
